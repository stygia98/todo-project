# PRD — Todo List 서비스

> **버전** 1.8 · **최종 수정** 2026-08-28
> 이 문서는 "무엇을 만드는가"를 정의한다. "어떻게 만드는가"는 `CLAUDE.md`, "언제 만드는가"는 `ROADMAP.md`를 참조한다.
>
> **완료 판정 기준은 `ROADMAP.md`의 검증 체크리스트가 정본이다.** 이 문서는 요구사항 정의만 담는다.

---

## 1. 배경과 목표

### 배경
기존 할 일 관리 도구는 단순 목록형(메모 수준)이거나 지나치게 복잡한 협업 도구(프로젝트 관리 수준) 둘 중 하나로 갈린다. 그 사이에 **"할 일마다 충분한 내용을 리치 텍스트로 적을 수 있으면서도 조작은 가볍게 끝나는"** 개인용 도구의 공백이 있다.

### 목표
1. 할 일에 서식 있는 본문을 남길 수 있게 한다 (링크, 목록, 코드 블록, 강조).
2. 완료 체크와 삭제가 **체감 지연 0에 가깝게** 동작하게 한다.
3. 이메일 또는 구글 계정으로 30초 안에 사용을 시작할 수 있게 한다.

### 비목표 (이번 범위 아님)
- 팀 공유, 협업, 댓글
- 알림, 리마인더, 이메일 발송
- 캘린더 뷰, 칸반 보드, 간트 차트
- 파일 첨부, 이미지 업로드 (→ **S3를 MVP 스택에서 제외한 이유**)
- 모바일 네이티브 앱
- 정렬 기준 선택 UI (생성일 내림차순 고정)
- 다크 모드 수동 토글 (OS 설정 추종까지만. 토글 추가 시 `class` 전략으로 전환 필요)

---

## 2. 대상 사용자

| 구분 | 설명 |
|---|---|
| 주 사용자 | 개인 업무/학습 일정을 스스로 관리하는 20~40대 |
| 사용 환경 | 데스크톱 브라우저 주 사용, 모바일 브라우저 보조 |
| 기술 수준 | 웹 서비스 사용에 익숙, 별도 학습 없이 바로 쓸 수 있어야 함 |

**핵심 시나리오**
> "오늘 처리할 일을 아침에 5~10개 적어두고, 각 항목에 참고 링크나 메모를 붙인다. 하루 동안 하나씩 체크하고, 끝난 항목은 지운다."

---

## 3. 기능 요구사항

**우선순위 정의** — **P0**: MVP 필수(전부 구현해야 완료) · **P2**: 범위 밖 기록

> **P1(있으면 좋음)은 쓰지 않는다.** MVP 범위에서 "있으면 좋은" 항목은 결국 만들지 않으면서 완료 판정만 흐린다. 그런 항목은 9장 「향후 확장 후보」로 옮긴다.

> 아래 P0 항목은 `ROADMAP.md`의 Phase 작업과 1:1로 대응한다. P0를 빼고 완료 처리하지 않는다.

### 3.1 인증 (AUTH)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| AUTH-01 | 이메일과 비밀번호로 회원가입할 수 있다. 이메일이 곧 로그인 ID다. | P0 |
| AUTH-02 | 비밀번호는 **6자 이상이며 UTF-8 기준 72바이트 이하**여야 하고, BCrypt로 해싱해 저장한다. 한계를 넘으면 500이 아니라 **400 안내**를 받는다. | P0 |
| AUTH-03 | 이미 가입된 이메일로 가입 시 중복 안내를 받는다. | P0 |
| AUTH-04 | 이메일과 비밀번호로 로그인하면 24시간 유효한 JWT를 받는다. | P0 |
| AUTH-05 | 구글 계정으로 로그인/가입할 수 있다. | P0 |
| AUTH-06 | 로그아웃하면 토큰이 제거되고 로그인 화면으로 이동한다. **서버 API 없이 클라이언트에서 토큰 삭제 + 캐시 초기화로 처리한다.** | P0 |
| AUTH-07 | 토큰이 없거나 만료된 상태로 보호된 화면에 접근하면 로그인 화면으로 이동한다. | P0 |
| AUTH-08 | 로그인 상태에서 내 닉네임을 공통 헤더에서 확인할 수 있다. 이메일은 `/auth/me` 응답에 포함되지만 **화면에는 표시하지 않는다**(`CLAUDE.md` 7장 헤더 규칙과 일치). | P0 |
| AUTH-09 | 같은 이메일의 로컬 계정이 이미 있으면 구글 로그인을 **거부하고** "이메일로 로그인해 주세요"를 안내한다. 반대 방향도 동일하다. | P0 |

**제외**: 비밀번호 재설정, 이메일 인증, Refresh Token, 회원 탈퇴, 계정 연동

### 3.2 할 일 관리 (TODO)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| TODO-01 | 제목을 입력해 할 일을 생성할 수 있다. 제목은 필수, 최대 200자. | P0 |
| TODO-02 | 본문을 Tiptap 에디터로 작성할 수 있다(굵게, 기울임, 제목 H2·H3, 불릿/번호 목록, 링크, 인라인 코드, 코드 블록, 인용). 본문은 선택. | P0 |
| TODO-03 | 우선순위를 HIGH / MEDIUM / LOW 중에서 지정할 수 있다. 기본값 MEDIUM. | P0 |
| TODO-04 | 마감일을 지정할 수 있다. 선택 항목. | P0 |
| TODO-05 | 내 할 일 목록을 **생성일 내림차순**으로 조회할 수 있다. | P0 |
| TODO-06 | 목록은 페이지당 10건씩 페이지네이션된다. | P0 |
| TODO-07 | 완료/미완료로 목록을 필터링할 수 있다. | P0 |
| TODO-08 | 제목 키워드로 목록을 검색할 수 있다. | P0 |
| TODO-09 | 할 일의 상세 내용을 조회할 수 있다. 상세 화면은 **진입 즉시 편집 가능**하다. | P0 |
| TODO-10 | 할 일의 제목·본문·우선순위·마감일을 수정하고 "저장" 버튼으로 반영한다. 자동 저장은 하지 않는다. **완료 상태는 이 화면에서 바꾸지 않는다**(목록의 체크박스 전용). | P0 |
| TODO-11 | 체크박스로 완료 상태를 토글할 수 있으며, **클릭 즉시 UI에 반영**된다. 연타해도 최종 상태가 서버와 어긋나지 않는다. | P0 |
| TODO-12 | 할 일을 삭제할 수 있으며, **클릭 즉시 목록에서 사라진다**. 실제로는 Soft Delete로 처리된다. | P0 |
| TODO-13 | 서버 요청이 실패하면 UI를 이전 상태로 되돌리고 실패를 알린다. | P0 |
| TODO-14 | 다른 사용자의 할 일은 조회·수정·삭제할 수 없다. | P0 |
| TODO-15 | 본문에 스크립트가 포함되어도 실행되지 않는다. 저장 시와 렌더 시 모두 정화한다. | P0 |
| TODO-16 | 수정 중 저장하지 않고 이탈하려 하면 확인을 받는다. | P0 |

### 3.3 화면 상태 (UX)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| UX-01 | 데이터 로딩 중에는 스켈레톤 UI를 보여준다. | P0 |
| UX-02 | 할 일이 하나도 없으면 안내 문구와 "할 일 추가" 버튼을 보여준다. | P0 |
| UX-03 | 검색/필터 결과가 없으면 빈 상태와 구분되는 문구를 보여준다. | P0 |
| UX-04 | 요청 실패 시 재시도 버튼이 있는 에러 상태를 보여준다. | P0 |
| UX-05 | 모바일(360px)부터 데스크톱까지 레이아웃이 깨지지 않는다. | P0 |
| UX-06 | 모든 폼 요소에 label이 연결되어 있고, 주요 동작을 키보드로 수행할 수 있다. | P0 |
| UX-07 | 디자인 토큰을 라이트/다크 양쪽으로 정의하고 OS 설정(`prefers-color-scheme`)을 따른다. | P0 |
| UX-08 | 사용자가 직접 테마를 전환하는 토글 UI. | P2 |

---

## 4. 사용자 플로우

### 4.1 신규 가입 → 첫 할 일 생성
```
/signup → 이메일·비밀번호·닉네임 입력 → 가입 성공 (JWT 발급)
  → /todos (빈 상태) → "할 일 추가" 클릭
  → /todos/new → 제목 입력 + 본문 작성 → 저장
  → /todos (항목 1개 표시)
```

### 4.2 구글 로그인
```
/login → "구글로 계속하기" 클릭
  → /oauth2/authorization/google → 구글 동의 화면
  → 백엔드 콜백
      ├─ 신규 이메일 → GOOGLE 계정 생성 → JWT 발급
      ├─ 기존 GOOGLE 계정 → JWT 발급
      └─ 동일 이메일의 LOCAL 계정 존재 → 거부
          → /login?error=email_conflict → 안내 문구 표시
  → /oauth/callback?token=xxx → 토큰 저장 + URL 정리
  → /todos
```

### 4.3 일상 사용
```
/todos → 체크박스 클릭 (즉시 완료 처리)
      → 항목 클릭 → /todos/[id] 편집 → 저장
      → 삭제 버튼 (즉시 목록에서 제거, Soft Delete)
      → 하단 페이지네이션으로 이전 항목 탐색
      → 헤더 로그아웃 → 토큰 삭제 → /login
```

---

## 5. 화면별 요구사항

### 5.0 요구사항 ID ↔ 화면 매핑

> **이 표가 3장과 5장의 정합성 기준이다.** 3장에 요구사항을 추가하면 이 표에 행을 먼저 추가하고, 대응 화면(또는 「전역」)에 실제로 기술한다. 표에 없는 ID가 3장에 있으면 그 요구사항은 구현되지 않는다.

| ID | 구현 위치 |
|---|---|
| AUTH-01, AUTH-02, AUTH-03 | `/signup` |
| AUTH-04, AUTH-09 | `/login` |
| AUTH-05 | `/login` → `/oauth/callback` |
| AUTH-06, AUTH-08 | 전역 (공통 헤더) |
| AUTH-07 | 전역 (라우트 보호) |
| TODO-01 ~ TODO-04 | `/todos/new`, `/todos/[id]` |
| TODO-05 ~ TODO-08 | `/todos` |
| TODO-09, TODO-10 | `/todos/[id]` |
| TODO-11 | `/todos` |
| TODO-12 | `/todos`, `/todos/[id]` |
| TODO-13 | `/todos`, `/todos/[id]` |
| TODO-14 | 전역 (소유권 검증) + `/todos/[id]` (404 화면) |
| TODO-15 | 전역 (XSS 정화) + `/todos/new`, `/todos/[id]` |
| TODO-16 | `/todos/new`, `/todos/[id]` |
| UX-01, UX-04 | `/todos`, `/todos/[id]` |
| UX-02, UX-03 | `/todos` |
| UX-05, UX-06, UX-07 | 전역 |
| UX-08 | — (P2, 범위 밖) |

---

### 5.1 전역 적용 요구사항 (모든 화면 공통)

> 특정 화면 하나에 속하지 않는 요구사항이다. 화면별 절에 중복 기술하지 않고 여기에만 둔다.

**공통 헤더** (인증이 필요한 모든 화면 — `/todos`, `/todos/new`, `/todos/[id]`)
- 닉네임 표시, 로그아웃 버튼 — `AUTH-06`, `AUTH-08`
- 로그아웃: 토큰 삭제 + React Query 캐시 초기화 → `/login` 이동. **서버 API 호출 없음**

**라우트 보호** — `AUTH-07`
- 토큰이 없거나 만료된 상태로 보호된 화면 접근 시 `/login`으로 이동
- 인증 판정이 끝나기 전에는 스켈레톤을 보여준다 (서버 렌더 결과와 어긋나지 않게)
- API 응답이 401이면 자동 로그아웃 처리

**소유권 · 404** — `TODO-14`
- 타인 소유이거나 존재하지 않는 리소스는 서버가 404로 응답한다(존재 여부를 노출하지 않음)
- 화면은 목록으로 자동 리다이렉트하지 않고 "찾을 수 없습니다" 상태를 보여준다

**XSS 정화** — `TODO-15`
- 서버가 저장 시 정화(Jsoup)하고, 프론트가 렌더 시 한 번 더 정화(DOMPurify)한다. 서버를 신뢰하더라도 이중 방어를 유지한다
- **프론트 정화의 적용 지점은 `editor.commands.setContent()` 직전이다.** 상세 화면이 "항상 편집 모드"이므로 이 앱에는 `dangerouslySetInnerHTML`을 쓰는 곳이 없다. 적용 지점을 명시하지 않으면 이중 방어가 실질적으로 사라진다
- 허용 태그 집합은 **툴바 · Tiptap 확장 · Jsoup · DOMPurify 네 곳이 항상 일치**해야 한다 (`CLAUDE.md` 6장·8장)

**반응형** — `UX-05`
- 360px ~ 1920px에서 레이아웃이 깨지지 않는다
- 컨테이너 `max-w-3xl`, 패딩 모바일 16px · 데스크톱 24px

**접근성** — `UX-06`
- 모든 입력에 label이 연결되어 있다
- 키보드만으로 가입·로그인·할 일 생성·완료 토글·삭제를 완주할 수 있다

**다크 모드** — `UX-07`
- 디자인 토큰을 라이트/다크 양쪽으로 정의하고 `prefers-color-scheme`을 따른다. 토글 UI는 두지 않는다

**에러 문구 매핑** (서버 `error.code` → 사용자에게 보이는 문구)

| 코드 | 화면 문구 | 표시 방식 |
|---|---|---|
| `INVALID_INPUT` | 서버가 준 필드별 메시지 | 해당 입력 아래 인라인 |
| `UNAUTHORIZED` (로그인 시) | "이메일 또는 비밀번호가 올바르지 않습니다." | 폼 상단 인라인 |
| `UNAUTHORIZED` (그 외) | 문구 없이 `/login`으로 이동 | — |
| `EMAIL_DUPLICATED` | "이미 사용 중인 이메일입니다." | 이메일 입력 아래 인라인 |
| `TODO_NOT_FOUND` | "할 일을 찾을 수 없습니다." | 전체 화면 상태 |
| `INTERNAL_ERROR` | "일시적인 오류가 발생했습니다. 다시 시도해 주세요." | 토스트 또는 에러 카드 |
| 네트워크 실패 | "연결에 실패했습니다." + 재시도 버튼 | 에러 카드 |

> 로그인 실패는 **계정 존재 여부를 구분해 노출하지 않는다.** 비밀번호 오류와 미가입 이메일에 같은 문구를 쓴다.

---

### 5.2 `/login`
- 이메일·비밀번호 입력, "로그인" 버튼 — `AUTH-04`
- "구글로 계속하기" 버튼 → `/oauth2/authorization/google`로 이동 — `AUTH-05`
- 회원가입 링크
- 실패 시 인라인 에러 메시지 (5.1 에러 문구 매핑 적용)
- `?error=email_conflict` 쿼리가 있으면 "이미 이메일로 가입된 계정입니다. 이메일로 로그인해 주세요." 표시 — `AUTH-09`

### 5.3 `/signup`
- 이메일·비밀번호·닉네임 입력 — `AUTH-01`
- 실시간 검증: 이메일 형식, **비밀번호 6자 이상 + UTF-8 72바이트 이하**, 닉네임 1~50자 — `AUTH-02`
  > 상한이 문자 수가 아니라 바이트인 이유는 BCrypt의 한계 때문이다. 한글은 1자가 3바이트이므로 **한글 25자면 이미 초과**한다. 안내 문구에 이 점을 밝힌다
- 중복 이메일은 서버 응답(`EMAIL_DUPLICATED`)으로 안내 — `AUTH-03`
- 가입 성공 시 JWT 저장 후 `/todos`로 이동

### 5.4 `/oauth/callback`
- 구글 인증 성공 후 백엔드가 `?token=xxx`를 붙여 302로 보내는 착지 화면 — `AUTH-05`
- 쿼리에서 `token`을 읽어 localStorage에 저장한다
- **저장 직후 URL에서 토큰을 제거**한다 (히스토리·공유 링크에 토큰이 남지 않도록)
- `/todos`로 이동한다
- 처리 중에는 스켈레톤을 보여준다. 사용자가 조작할 요소는 두지 않는다
- `token`이 없거나 비어 있으면 `/login`으로 보낸다
- ⚠️ `useSearchParams`를 쓰므로 **`<Suspense>` 경계가 필수**다 (없으면 빌드가 실패한다)
- 공통 헤더를 두지 않는다 (인증 처리 중 화면)

### 5.5 `/todos`
- 상단: 검색 입력, 완료 여부 필터, "할 일 추가" 버튼 — `TODO-07`, `TODO-08`
- 목록: 체크박스 · 제목 · 우선순위 뱃지 · 마감일 · 삭제 버튼
- **목록은 생성일 내림차순으로 표시한다** — `TODO-05`
- 완료 항목은 제목에 취소선 + 흐린 색상
- 하단: 페이지네이션, 페이지당 10건 (공용 컴포넌트) — `TODO-06`
- 체크박스 클릭 시 즉시 UI 반영 — `TODO-11`
- 삭제 버튼 클릭 시 즉시 목록에서 제거 — `TODO-12`
- **토글·삭제 요청이 실패하면 이전 상태로 롤백하고 `sonner` 토스트로 알린다** — `TODO-13`
- 마지막 항목 삭제로 현재 페이지가 비고 `page > 0`이면 이전 페이지로 이동한다 (빈 상태를 띄우지 않는다)
  - **이동은 서버 삭제가 확정된 뒤에 수행한다.** 낙관적 제거로 화면은 즉시 비지만 URL은 유지한다. 먼저 이동해 버리면 삭제 실패 시 롤백이 사용자가 보지 않는 페이지에 적용되어 `TODO-13`을 만족하지 못한다
- 상태별 화면 — `UX-01` ~ `UX-04`
  - 로딩: 항목형 스켈레톤 3개
  - 빈 상태: 아이콘 + "아직 할 일이 없어요" + "할 일 추가" 버튼
  - 검색 결과 없음: 빈 상태와 **구분되는 문구**
  - 에러: 재시도 버튼이 있는 인라인 에러 카드
- **정렬 선택 UI는 두지 않는다** (생성일 내림차순 고정)
- ⚠️ 검색어·필터·페이지를 URL 쿼리로 관리하므로 **`<Suspense>` 경계가 필수**다

### 5.6 `/todos/new`, `/todos/[id]`
- **`TodoForm` 컴포넌트를 재사용**한다. 초기값 유무와 삭제 버튼 노출로만 구분한다.
- 제목 입력 (필수, 200자) — `TODO-01`
- Tiptap 에디터 (툴바: 굵게, 기울임, 제목 H2·H3, 불릿/번호 목록, 링크, 인라인 코드, 코드 블록, 인용) — `TODO-02`
- 툴바에 없는 서식(제목 H1, 취소선, 구분선)은 입력 규칙으로도 생성되지 않아야 한다 — 저장 후 소실 방지
- 우선순위 선택, 마감일 선택 — `TODO-03`, `TODO-04`
- **완료 체크박스는 두지 않는다.** 완료 상태는 목록에서만 바꾼다 — `TODO-10`
- 저장 / 취소 버튼, 상세에서는 삭제 버튼 추가
- 변경 사항이 있는 상태로 이탈 시 확인 대화상자 — `TODO-16`

**`/todos/[id]` 전용**
- 진입 즉시 모든 필드가 편집 가능하다. 보기/편집 모드를 나누지 않는다 — `TODO-09`
- 로딩 중에는 폼 형태 스켈레톤을 보여준다 — `UX-01`
- **존재하지 않거나 타인 소유인 id로 접근하면**(서버 404) "할 일을 찾을 수 없습니다" 화면과 **"목록으로 가기" 버튼**을 보여준다. 목록으로 자동 리다이렉트하지 않는다 — `TODO-14`
  > Next.js `notFound()`를 쓰지 않는다. 클라이언트 데이터 페칭이라 동작하지 않는다.
- 저장 실패 시 폼 내용을 유지한 채 에러를 표시한다 — `TODO-13`, `UX-04`
- **삭제 성공 시 `/todos`로 이동**한다 — `TODO-12`

---

## 6. 데이터 요구사항

| 대상 | 규칙 |
|---|---|
| 사용자 식별 | 이메일 (유일, provider와 무관하게 중복 불가) |
| 비밀번호 | **6자 이상 + UTF-8 72바이트 이하**, BCrypt 해싱, 평문 저장 금지 |
| 닉네임 | 1~50자. 구글 가입 시 구글 `name` → 없으면 이메일 `@` 앞부분 |
| 할 일 제목 | 1~200자, 필수 |
| 할 일 본문 | Tiptap HTML, **최대 50,000자**, 선택, **저장 전 정화 필수** |
| 삭제 | 물리 삭제 금지. `deleted_at` 기록 (Soft Delete) |
| 조회 | 모든 조회는 `deleted_at IS NULL` + 본인 소유만 |
| 정렬 | 생성일 내림차순 고정 |
| 검색 | 제목 부분 일치, **대소문자 무시** |
| 완료 상태 | 목록 체크박스로만 변경. 수정 화면에서는 다루지 않음 |

상세 스키마는 `CLAUDE.md` 4장 참조.

---

## 7. 비기능 요구사항

| 항목 | 기준 | 검증 위치 |
|---|---|---|
| 응답 속도 | **키워드 검색 포함** 목록 조회 500ms 이내 (로컬, **시드 10,000건**, 워밍업 후 중앙값) | Phase 4 |
| 체감 반응 | 완료 토글·삭제는 낙관적 업데이트로 즉시 반영 | Phase 9 |
| 브라우저 | **Chromium 계열 1종 + 사용 가능한 다른 엔진 1종** (Windows: Chrome + Edge/Firefox, Mac: Chrome + Safari) | Phase 10 |
| 반응형 | 360px ~ 1920px (Phase 8은 360px, Phase 10은 상한까지) | Phase 8·10 |
| 인증 보안 | JWT 인증, 소유권 검증, 입력값 검증 | Phase 3·4 |
| 시크릿 관리 | 환경변수로 분리, 저장소에 커밋하지 않음 | Phase 10·11 |
| XSS 방어 | 저장 시 서버 정화 + 렌더 시 DOMPurify 이중 방어 | Phase 4·8 |
| 문서화 | 모든 API가 Swagger UI에 노출 | Phase 4 |
| 접근성 | 폼 label 연결, 키보드로 주요 동작 가능 | Phase 7·8 |

> 검증 위치가 없는 요구사항은 검증되지 않는다. 새 요구사항을 추가할 때 반드시 Phase를 함께 지정한다.

---

## 8. 완료 판정

MVP 완료 여부는 **`ROADMAP.md` Phase 10의 검증 체크리스트**로 판정한다. 이 문서에는 별도의 성공 기준을 두지 않는다. (두 곳에 두면 반드시 갈라지기 때문)

---

## 9. 향후 확장 후보 (범위 밖)

우선순위 순으로 기록만 해둔다. 이번 MVP에서는 구현하지 않는다.

1. 마감일 기준 정렬 및 "오늘 할 일" 뷰 (`sort` 파라미터 활용)
2. 태그/카테고리 분류
3. 휴지통 (Soft Delete 복원)
4. 다크 모드 수동 토글
5. 할 일 순서 드래그 정렬
6. 비밀번호 재설정 및 이메일 인증
7. 소셜 계정 연동 (본인 확인 절차 포함)
8. 이미지 첨부 (S3 도입 시점)
9. 카카오 소셜 로그인
10. 주간 완료 통계
